# Sprint 14C Pass 9 Notes

Pass 9 begins the Queue UI presenter/controller extraction by moving active queue strip presentation and selected queue ID lookup into `QueueUiPresenter`.

The next queue pass can move more of `createQueueWidget()`, queue filter wiring, and queue action button ownership once this first presenter slice is stable.
