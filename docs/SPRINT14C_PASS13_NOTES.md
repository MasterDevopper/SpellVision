# Sprint 14C Pass 13 Notes

- Moves model catalog scanning and video stack discovery to `AssetCatalogScanner`.
- Keeps `ImageGenerationPage` responsible for page state and picker actions.
- Preserves model picker, LoRA picker, video stack discovery, generation submission, and preview behavior.
